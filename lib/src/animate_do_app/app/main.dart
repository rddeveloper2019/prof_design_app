import 'package:flutter/material.dart';

import '../animate_do_app.dart';

class AnimateDoApp extends StatelessWidget {
  const AnimateDoApp({super.key});

  @override
  Widget build(BuildContext context) {
    return FirstPage();
  }
}
