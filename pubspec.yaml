name: prof_design_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_svg: ^2.3.0
  flutter_staggered_grid_view: 0.4.1
  font_awesome_flutter: ^11.0.0
  animate_do: ^5.1.0
  provider: ^6.1.5+1
  just_audio: 0.10.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/slides/
    - assets/images/
    - assets/musics/music.mp3
